{
  "name": "@fink/website",
  "version": "0.0.0-semantically-released",
  "license": "MIT",
  "engines": {
    "node": ">=14.0.0"
  },
  "private": true,
  "repository": {
    "type": "git",
    "url": "git@github.com:fink-lang/fink-lang.github.io"
  },
  "bugs": "https://github.com/fink-lang/website/issues",
  "homepage": "https://github.com/fink-lang/website",
  "scripts": {
    "default": "run server",
    "server": "next dev",
    "clean": "rimraf ./build",
    "build": "run build:*",
    "build:dir": "mkdir -p ./build/",
    "build:next": "next build",
    "build:export": "next export -o build",
    "test": "run jest",
    "jest": "jest --verbose --runInBand --no-cache ",
    "deploy": "aws s3 sync ./build/  s3://fink-lang.org/",
    "cd": "run clean test build release deploy",
    "release": "semantic-release"
  },
  "devDependencies": {
    "@fink/cli": "^8.3.0",
    "@fink/larix": "^18.3.0",
    "@fink/loxia": "^21.3.0",
    "@fink/std-lib": "^8.1.0",
    "@mdx-js/loader": "^1.6.16",
    "@next/mdx": "^11.0.0",
    "commitizen": "^4.2.1",
    "cz-conventional-changelog": "^3.3.0",
    "fink-loader": "^3.3.0",
    "jest-cli": "^27.0.0",
    "next": "^11.0.0",
    "npx-run": "^2.1.2",
    "raw-loader": "^4.0.1",
    "react": "^17.0.1",
    "react-dom": "^17.0.0",
    "semantic-release": "^17.1.1"
  },
  "config": {
    "commitizen": {
      "path": "./node_modules/cz-conventional-changelog"
    }
  }
}
